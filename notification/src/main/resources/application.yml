spring:
    application:
        name: notification
    data:
        mongodb:
            host: localhost
            port: 27017
            database: notification
            authentication-database: admin
server:
    port: 8071

api:
    version : /v01
    service: /notification